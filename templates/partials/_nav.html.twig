<div class="containerNavbar">
	<link rel="stylesheet" href="stylesNavbar.css">
	<nav class="navbar navbar-expand-lg navbar-light bg">
		<div class="container-fluid">
<a class="navbar-brand" href="{{path('app_post_index')}}">

				{# <img class="logoQuai" src="../../../assets/pictures/logo.png" alt="logo_quai_antique"> #}
			</a>
			<div class="d-flex justify-content-center">
				<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
					<span class="navbar-toggler-icon"></span>
				</button>
			</div>
			<div class="d-flex justify-content-center">
				{% if app.user %}
					{% if is_granted('ROLE_ADMIN') %}
						<a href="{{ path('app_admin') }}" class="nav-link-register">Admin</a>
					{% else %}
						<a href="{{ path('app_account_index') }}" class="nav-link-register">Compte</a>
					{% endif %}
					<a class="nav-link" href="{{path('app_logout')}}">
						{# <img class="cadenas" src="../../../assets/pictures/lock.png" alt="logo_cadenas"> #}
					</a>
				{% else %}
					<a class="nav-link" href="{{path('app_login')}}">
						{# <img class="cadenas" src="../../../assets/pictures/unlock.png" alt="logo_cadenas"> #}

					</a>
				{% endif %}
			</div>
			<div class="collapse navbar-collapse" id="navbarNav">
				<ul class="navbar-nav">
					<li class="nav-item">
<a class="nav-link active" aria-current="page" href="{{path('app_post_index')}}">Accueil</a>

					</li>
					<li class="nav-item">
						<a class="nav-link " aria-current="page" href="{{ path('app_register') }}">S'inscrire</a>
					</li>
				</ul>
			</div>
		</div>
	</nav>
</div>
