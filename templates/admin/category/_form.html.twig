{{ form_start(menuFormulaire, {'attr': {'id': 'monFormulaire'}}) }}

{{ form_row(menuFormulaire.name) }}
{{ form_row(menuFormulaire.description) }}
{{ form_row(menuFormulaire.price) }}
<button class="btn-style primary">Publier</button>
<a href="{{ path('app_admin') }}" class="btn btn-primary">Panneau d'administration</a>


{{ form_end(menuFormulaire) }}

